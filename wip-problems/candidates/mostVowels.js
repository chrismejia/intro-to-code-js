/**
 * Define a function, `mostVowels`, that accepts one argument, a string of words.
 * `mostVowels` should return the word that has the most vowels.
 * If none of the words have any vowels, return an empty string.
 * If more than one word in the phrase has the
 * @function mostVowels
 * @param {String} phrase
 * @returns {String|String[]}
 * @example
 * mostVowels('try my gym'); // => '' -- no vowels
 * mostVowels('I am a keeper with some real rhythms'); // => 'keeper' -- single most, with 3
 * mostVowels("Sixteen rogue elephants trampled the neighbor's house".) // ['sixteen', 'rogue', 'elephants', "neighbor's", "house"]
 */

function mostVowels(phrase) {}
