/**
 * Define the object method `repeatStr` within the wrapper object.
 * `repeatStr` accepts two inputs, a string, `strInput`, and a number, `times`.
 *
 * `repeatStr` returns a string repeated `times` times.
 * When `times` is 0, `repeatStr` returns `strInput`.
 *
 * You are NOT allowed to use `String.repeat()`.
 *
 * NOTE: Recursive calls need to be `wrapper.repeatStr` NOT `repeatStr`!
 *
 * @category 08 - Recursion
 * @function repeatStr
 * @param {String} strInput
 * @param {Number} times
 * @returns {String} the final repeated string.
 */

export const wrapper = {
  repeatStr: function () {},
};
